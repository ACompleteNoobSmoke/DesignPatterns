/*Jenkinsfile (Declarative Pipeline) */
/* Requires the Docker Pipeline plugin */
pipeline {
    agent any 
    stages {
        stage('Hello-World') {
            steps {
                sh 'java --version'
                sh 'Hello World'
            }
        }
    }
}
